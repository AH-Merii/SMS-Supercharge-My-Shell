# 1Password keeps nothing we would edit, so the declaration is the whole program. The AUR
# package is 1Password's own, unlike the community-maintained 1password-cli beside it.
{
  tier = "desktop";
  install.linux.aur = "1password";
}
