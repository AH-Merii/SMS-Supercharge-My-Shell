# apt packages for the base profile on Debian/Ubuntu.
# Servers install all of it. On WSL the Brewfile is the source for everything it also
# names (fish, git, stow, tmux, gnupg, luarocks, wget: Ubuntu's fish is too old), and
# `deps` drops those from this list (apt_pkgs in lib/plan.sh), so apt only covers the rest.
build-essential
curl
git
stow
fish
tmux
gnupg
luarocks
wget
unzip
