# AUR packages for the desktop profile, installed with paru when available.
1password
# Community-maintained, unlike 1password above which 1Password itself maintains on
# the AUR. Its PKGBUILD automates 1Password's documented manual install (same CDN
# URL, same `gpg --verify op.sig op`, same codesign key), so trust rests on their
# signature rather than the packager -- but skim the PKGBUILD diff paru shows. That
# review survives `mise run setup`; only an explicit -y / SMS_YES=1 skips it.
1password-cli
phinger-cursors
# Codex ships inside the ChatGPT desktop app, so there is no package named "codex".
# Same trust shape as 1password-cli above: the PKGBUILD downloads the deb OpenAI
# publishes on its own CDN and pins the sha256 that OpenAI's APT repo advertises for
# it, so trust rests on that binary rather than on the packager -- skim the PKGBUILD
# diff paru shows. Preferred over cachyos/chatgpt-desktop-bin, which repackages the
# same upstream but trails it (26.901.20858 vs 26.901.51231 as of 2026-09-07) and so
# can sit on a build predating a model launch.
chatgpt-desktop
